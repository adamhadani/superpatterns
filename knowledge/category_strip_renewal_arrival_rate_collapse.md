# Category: Strip Renewal Arrival Rate Collapse

Type: failed_approach_category
Pattern: Dedicating each target element to a continuous horizontal rank strip or band of height $1/k$ or $1/(2k)$ collapses 2D point acquisition to a 1D Poisson renewal process of linear rate $C k$, forcing expected horizontal traversal distance $\mathbb{E}[x_k] = 1/C \ge 3.33 \text{ to } 8.0 \gg 1.0$ and collapsing completion probability inside $[0, 1]$ to an exponentially suppressed lower tail $\le 10^{-50}$.
Count: 26

## Entries
| # | Original File | One-line Summary |
|---|---------------|------------------|
| 1 | explorer_r2_L00_N00 | Restricting point selection to $k$ disjoint horizontal rank bands collapses 2D Poisson optimization to 1D Poisson renewal of rate $C k$, forcing expected horizontal traversal span $\mathbb{E}[x_k] = 1/C = 4.0 \gg 1.0$ at $C = 1/4+\varepsilon$, with greedy selection provably optimal across disjoint bands. |
| 2 | explorer_r2_L00_N03 | Single-leaf horizontal foliation of linear intensity $\mu_{\mathrm{leaf}} = (1/4+\varepsilon)k$ has expected traversal distance $\mathbb{E}[x_k] = 1/C \ge 3.333$ to $3.846$, overshooting host interval $[0, 1]$ by $3.3\times$ to $3.8\times$ and failing with probability $\ge 1 - 10^{-220}$. |
| 3 | explorer_r2_L00_N05 | Dedicated horizontal strips of height $1/(2k)$ have 1D arrival rate $\rho = (1/8+\varepsilon/4)k$, requiring expected horizontal distance $\mathbb{E}[X_k] = 8/(1+2\varepsilon) \approx 7.27 \gg 1.0$ and failing with probability $\ge 1 - 10^{-50}$ at $k=100$. |
| 4 | explorer_r2_L00_N06 | 1D horizontal strip scanning at rate $C k$ requires expected horizontal span $k/(C k) = 1/C \approx 3.33 > 1.0$ at $C = 0.30$, forcing an intrinsic 1D arrival rate barrier at $C \ge 1.0$. |
| 5 | explorer_r2_L00_N07 | Equating 1D corridor Poisson arrival rate $C k$ with 2D LIS traversal velocity $2\sqrt{C}$ is a category error; true expected horizontal increment is $1/(C k) = 3.333/k$ at $C = 0.30$, giving expected span $\mathbb{E}[x_k] = 3.333 \gg 1.0$ and embedding only $30\%$ of target elements before exiting $[0, 1]^2$. |
| 6 | explorer_r2_L00_N10 | Continuous greedy scanning in narrow rank bands has Poisson renewal arrival rate $\lambda = (1+2\varepsilon)/4$, giving expected host distance $\mathbb{E}[X_k] \approx 4/(1+2\varepsilon) \approx 4.0 \gg 1.0$ and single-thread failure rate $1 - \exp(-0.636k) \approx 1.0$. |
| 7 | explorer_r2_L00_N13 | Rigid rank bands have Poisson arrival rate $Ck$, requiring $C \ge 1.0$; at $C = 1/4+\varepsilon$, the renewal process overshoots $[0, 1]$ and starves with probability $1 - 10^{-23}$. |
| 8 | explorer_r2_L00_N15 | 1D strip arrival rate $\lambda = (C/2)k$ gives mean jump $\mathbb{E}[X_k] = 8.0 \gg 1.0$, causing candidate chain starvation with probability $1 - 5.26 \times 10^{-49}$ at $k=100$. |
| 9 | explorer_r2_L00_N16 | 1D strip rate requires $C \ge 2.0$ ($\mathbb{E}[X_k] = 2/C = 8.0$ at $C=0.25$), starving point arrivals within $[0, 1]$. |
| 10 | explorer_r2_L00_N18 | Slicing $[0, 1]^2$ into $2k$ horizontal strips of height $1/(2k)$ gives 1D Poisson rate $\lambda = (1/8+\varepsilon/4)k$, forcing expected traversal span $\mathbb{E}[x_k] = 2/C = 8/(1+2\varepsilon) \approx 8.0 \gg 1.0$, requiring $C \ge 2.0$ (8x above Alon's threshold). |
| 11 | explorer_r2_L00_N21 | 1D horizontal corridors of height $1/(2k)$ have arrival rate $\lambda = (1/8+\varepsilon/2)k$ with expected completion distance $\mathbb{E}[x_k] = 8/(1+4\varepsilon) \approx 8.0 \gg 1.0$, suffering an $800\%$ geometric capacity deficit and discarding $87.5\%$ of Poisson points. |
| 12 | explorer_r2_L00_N22 | Rigid 1D rank bands of height $1/k$ have Poisson arrival rate $\rho = (1/4+\varepsilon/2)k$, yielding expected horizontal completion coordinate $\mathbb{E}[x_k] = 1/C \approx 3.64$ to $4.0 > 1.0$, requiring $C > 1.0$. |
| 13 | explorer_r2_L00_N25 | Pre-assigned horizontal rank tracks of height $1/(2k)$ have arrival rate $\lambda = (1/8+\varepsilon/2)k$ and expected completion distance $\mathbb{E}[x_k] = 8/(1+4\varepsilon) \approx 6.67 \gg 1.0$; greedy search is the exact pointwise lower bound ($x_j \ge g_j$ for all $j$), so lookahead cannot reduce traversal time. |
| 14 | explorer_r2_L00_N26 | 1D strip renewal at height $1/(2k)$ has arrival rate $\lambda = (C/2)k$, yielding expected horizontal completion coordinate $\mathbb{E}[X_k] = 2/C \approx 8.0 \gg 1.0$, embedding only $12.5\%$ of target elements within $[0, 1]^2$. |
| 15 | explorer_r2_L00_N27 | Campbell's formula proves expected embedding count in global rank strips of width $1/k$ is $\mathbb{E}[N_\pi] = (C k)^k / k! \le (e C)^k / \sqrt{2\pi k}$; for $C = 1/4+\varepsilon/2 = 0.275$, $e C \approx 0.7476 < 1$, forcing $\mathbb{E}[N_\pi] \to 0$ exponentially ($9.19 \times 10^{-15}$ at $k=100$) and non-containment probability $\Pr(N_\pi = 0) \to 1.0$. |
| 16 | explorer_r2_L00_N28 | Strips of height $H/k$ achieve only $\mathbb{E}[X_k] \approx 3.64$, embedding at most $27.5\%$ of elements before overshooting host boundary $[0, 1]$. |
| 17 | explorer_r2_L00_N29 | Slicing $[0, 1]^2$ into $k$ disjoint horizontal strips yields 1D Poisson arrival rate $C k$, forcing $\mathbb{E}[X_k] = 1/C \approx 4.0 > 1.0$ and single-thread success $\le 1.23 \times 10^{-29}$ at $k=100$. |
| 18 | explorer_r2_L00_N30 | 1D strips of height $1/(2k)$ require expected horizontal distance $\mathbb{E}[X_k] = 8/(1+2\varepsilon) \approx 7.27 > 1.0$, embedding only $13.8\%$ of elements before reaching $x=1.0$. |
| 19 | strategy_L01_N00 | Slicing into $k$ disjoint horizontal rank bands collapses point acquisition to 1D Poisson renewal of rate $C k$, forcing $\mathbb{E}[x_k] \ge 1/C = 3.64 \gg 1.0$; greedy selection is pointwise optimal ($x_j \ge g_j$), so lookahead cannot bypass traversal collapse. |
| 20 | strategy_L01_N03 | Any disjoint strip partition $\sum h_i \le 1$ forces $\sum 1/h_i \ge k^2$ by Cauchy-Schwarz, requiring $\mathbb{E}[X_k] = \frac{1}{C k^2} \sum 1/h_i \ge 1/C \ge 3.64$; lookahead/DP cannot beat greedy arrival. |
| 21 | strategy_L01_N06 | Slicing $[0, 1]^2$ into 1D horizontal strips collapses point arrivals to rate $C k$, where greedy pointwise lower bound $x_j \ge g_j$ holds and enforces $\mathbb{E}[x_k] \ge 1/C > 1.0$. |
| 22 | strategy_L01_N07 | Cauchy-Schwarz inequality forces expected horizontal span $\mathbb{E}[x_k] = \frac{1}{Ck^2}\sum 1/h_i \ge 1/C = 3.64 \gg 1.0$, requiring host constant $C \ge 1.0$. |
| 23 | strategy_L01_N10 | Non-uniform strip assignments cannot escape the Cauchy-Schwarz barrier $\sum 1/h_i \ge k^2$, and greedy selection achieves the exact pointwise minimum $x_j \ge g_j$. |
| 24 | strategy_L01_N11 | Non-uniform strip heights strictly worsen the Cauchy-Schwarz lower bound ($\sum 1/h_i \ge k^2$ is strictly minimized at uniform heights $h_i = 1/k$), increasing traversal delay. |
| 25 | strategy_L01_N14 | Cauchy-Schwarz inequality proves any non-uniform strip height distribution has $\sum 1/h_i > k^2$, making 1D traversal strictly worse than uniform spacing and failing $\mathbb{E}[x_k] \le 1.0$. |
| 26 | explore_merger_r3_L03_N01 | Dedicating target elements to disjoint horizontal rank strips of height $1/k$ collapses point acquisition to rate $C k$, forcing expected horizontal span $\mathbb{E}[X_k] \ge 1/C = 3.6364 > 1.0$ at $C = 0.275$ with completion probability $\le 2.63 \times 10^{-25}$ at $k=100$ and $\le 10^{-245.8}$ at $k=1000$, while Campbell product formula confirms $\mathbb{E}[N_\pi] \to 0$ exponentially for all $C < 1/e \approx 0.3679$. |
