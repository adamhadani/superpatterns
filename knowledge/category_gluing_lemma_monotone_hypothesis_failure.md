# Category: Gluing Lemma Monotone Hypothesis Failure

Type: failed_approach_category
Pattern: Boundary-Compatible Gluing (Lemma 3.5 of W47) explicitly requires every block to be a monotone block of contiguous positions and contiguous values, which fails for dyadic spatial intervals on over 99.98% of permutations in $S_k$.
Count: 29

## Entries
| # | Original File | One-line Summary |
|---|---------------|------------------|
| 1 | explorer_L00_N00 | Applied Lemma 3.5 to general dyadic intervals; combinatorial verification shows only 8 of 40,320 permutations in $S_8$ (0.0198%) satisfy monotone contiguous-value blocks. |
| 2 | explorer_L00_N08 | Misapplication of W47 Lemma 3.5 without checking monotone contiguous-value hypothesis; general dyadic blocks fail hypothesis on $> 99.9\%$ of permutations. |
| 3 | strategy_L02_N01 | Boundary-compatible gluing lemma fails on generic permutations due to absence of monotone blocks $\ge 3$ (simple permutations asymptotic $1-2/k$). |
| 4 | strategy_L02_N04 | Macroscopic skeletal tier $\mathcal{M}$ is empty for generic permutations, forcing 100% of elements into the un-gluable residual $\mathcal{R}$. |
| 5 | strategy_L03_N01 | Section 4 Lemma 4.1 assumes $\pi$ is partitioned into monotone interval blocks, providing zero coordinate separation or gluing mechanism for residual components $\mathcal{R}$ which constitute $100\%$ of generic permutations. |
| 6 | strategy_L04_N00 | Active Obligation 03 claimed solved via Lemma 4.1, but Lemma 4.1 explicitly requires pairwise disjoint value intervals; generic permutations have $>95\%$ to $99.6\%$ vertical span overlap and $\mathcal{M} = \emptyset$ almost surely, gluing 0 generic elements. |
| 7 | strategy_L01_N00 | Alternating permutations $21^{\oplus (k/2)}$ have $L_2 = k-2 \gg 3\sqrt{k}$ (excluded from quasirandom $\mathcal{Q}_k$) and max monotone block 2 (excluded from monotone inflations $\mathcal{M}_{\mathrm{infl}}$), falling into an unbridgeable coverage void between Tier 1 and Tier 2. |
| 8 | strategy_L01_N01 | Extremal coverage void: alternating permutations $21^{\oplus (k/2)}$ have $L_2 = k-2 \gg 3\sqrt{k}$ (violating quasirandomness) and max monotone block 2 (violating monotone inflation hypothesis $L \ge K\sqrt{\log k}$), leaving them uncovered by two-tier master architectures. |
| 9 | strategy_L01_N05 | Alternating permutations $21^{\oplus (k/2)}$ are excluded from both monotone inflations (max block 2) and quasirandom permutations ($L_2 = k-2$), falling into an unbridgeable coverage void. |
| 10 | strategy_L01_N07 | Quasirandom vs structured trichotomy leaves alternating permutations $21^{\oplus (k/2)}$ in an unbridgeable coverage void between $\mathcal{M}_{\mathrm{infl}}$ and $\mathcal{Q}_k$. |
| 11 | strategy_L01_N13 | Alternating permutations $21^{\oplus (k/2)}$ fall into the coverage void between monotone inflations and quasirandom permutations, lacking monotone blocks for gluing. |
| 12 | strategy_L01_N15 | Alternating permutations $21^{\oplus (k/2)}$ have $L_2 = k-2 \gg 3\sqrt{k}$ and max monotone block 2, falling into an unbridgeable coverage void between Tier 1 ($\mathcal{M}_{\mathrm{infl}}$) and Tier 2 ($\mathcal{Q}_k$). |
| 13 | strategy_r2_L03_N01 | Claiming Tier 1 shared squares resolve Alon's conjecture for structured inflations ignores that generic permutations are simple almost surely ($0.00\%$ have blocks $\ge 4$), covering an asymptotically measure-zero set. |
| 14 | strategy_r2_L03_N04 | Definition 5.1 vs Lemma 6.2 partition mismatch: Definition assigns any permutation with at least one run $\ge L_0$ to $\mathcal{M}_{\mathrm{infl}}$ (97.74% in $S_{50}$), but Lemma 6.2 assumes 100% partitionability into large blocks (0/5000 in $S_{50}$), orphaning permutations with isolated runs; furthermore guard corridors between $m$ squares cause horizontal coordinate overrun $1 + (m-1)\Delta/k > 1.0$. |
| 15 | strategy_r2_L04_N00 | Theorem 6 requires monotone blocks to have disjoint vertical intervals for candidate square embedding, but Definition 5 defines $\mathcal{M}_{\mathrm{infl}}(\varepsilon)$ purely by domain partition; permutations with interleaved monotone runs (e.g. $(1, 3, 5, \dots, 2, 4, 6, \dots)$) satisfy Definition 5 yet cannot be embedded into disjoint host squares, leaving them orphaned, while true interval inflations are asymptotically measure-zero ($\le 10^{-2560.2}$ of $S_{1000}$). |
| 16 | explorer_r3_L00_N09 | True interval inflations cover an asymptotically measure-zero set ($\le 10^{-2560}$ of $S_{1000}$), leaving $>99.999\%$ of $S_k$ orphaned into fatal debts. |
| 17 | explorer_r3_L00_N11 | Multi-slope interleaved counterexample (500 increasing odd, 500 decreasing even) has $\operatorname{LIS}=501$ and $\operatorname{LDS}=500$, refuting claim that contiguous monotone runs bound Dilworth width $d = O(1)$. |
| 18 | explorer_r3_L00_N12 | Conditioning on Greene-Dilworth chain width $d \le D(\varepsilon)$ restricts to an asymptotically measure-zero set ($\le 10^{-567}$ at $k=1000$). |
| 19 | explorer_r3_L00_N14 | Erdős-Szekeres contradiction: defining $Q_k(\varepsilon)$ by $\operatorname{LIS} < 4/\varepsilon$ and $\operatorname{LDS} < 4/\varepsilon$ violates $\operatorname{LIS} \cdot \operatorname{LDS} \ge k$ for all $k > 16/\varepsilon^2$ ($k > 6400$ at $\varepsilon=0.05$), rendering $Q_k$ completely empty. |
| 20 | N18 (Level 0) | Tier 1 restricted to true interval inflations covers $\le 10^{-2560}$ of $S_{1000}$, leaving multi-run permutations orphaned |
| 21 | N24 (Level 0) | Claimed $D_4$ inversion duality ($M_{\mathrm{int}}^* \equiv M_{\mathrm{int}}$) fails because true interval blocks require both domain and range to be contiguous intervals; $\pi = (1, 3, \dots, 2, 4, \dots)$ has block size 1 in both $\pi$ and $\pi^{-1}$ |
| 22 | N26 (Level 0) | Restricting Tier 1 to true interval inflations covers $\le 10^{-2159}$ of $S_{1000}$, leaving interleaved monotone runs completely orphaned |
| 23 | N27 (Level 0) | Restricting Tier 1 to true interval inflations covers $\le 10^{-2560}$ of $S_{1000}$, and dumping interleaved permutations into unproved variational DAG leaves them orphaned |
| 24 | N29 (Level 0) | Restricting Tier 1 to true interval inflations leaves an asymptotically measure-zero set ($\le 10^{-2560}$ of $S_{1000}$), shunting 100% of practical permutations into broken dynamic quantile selection |
| 25 | N31 (Level 0) | Restricting Tier 1 to true interval inflations covers $\le 10^{-2560}$ of $S_{1000}$, and re-routing interleaved runs to $S_{\mathrm{chains}}$ points to an unproved debt |
| 26 | explore_merger_r3_L01_N04 | $D_4$ transposition invariance trick ($\pi \leftrightarrow \pi^{-1}$) fails because true interval blocks require contiguous domain AND range, making $\mathcal{M}_{\mathrm{int}}^* \equiv \mathcal{M}_{\mathrm{int}}$ identically, so transposition creates zero new interval blocks. |
| 27 | explore_merger_r3_L01_N09 | Definition 2.4 claim that contiguous monotone runs imply bounded Dilworth chain width $d = O(1)$ is refuted by contiguous decreasing runs which have $\operatorname{LDS}(\pi) = k/2 = \Theta(k)$, requiring $\Theta(k)$ increasing chains rather than $O(1)$. |
| 28 | explore_merger_r3_L01_N13 | The pair capacity bound $2 L_{21} \le \operatorname{LIS}$ is mathematically false, disproved by counterexample $\sigma = [7, 8, 4, 6, 5, 2, 1, 10, 9, 3]$ where $2 L_{21} = 4 > 3 = \operatorname{LIS}$. |
| 29 | explore_merger_r3_L01_N14 | Restricting Tier 1 to true monotone interval inflations $\mathcal{M}_{\mathrm{interval}}(\varepsilon)$ with blocks $a_i \ge L_0 \ge 320$ reduces its scope to an asymptotically measure-zero set: at $k=1000$, exactly 8,334 permutations qualify, representing a fraction $\le 10^{-2563.68}$ of $S_{1000}$ (by Albert, Atkinson, Klazar 2003 simple permutation density $\to 1.0$). |
