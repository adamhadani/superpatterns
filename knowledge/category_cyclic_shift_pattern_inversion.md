# Category: Cyclic Shift Pattern Inversion

Type: failed_approach_category
Pattern: Defining scanning threads via cyclic value shifts $\pi(j) + t \pmod k$ strictly inverts the relative vertical order of the extreme value pair $(\pi^{-1}(k), \pi^{-1}(1))$ for 100% of target permutations and all non-zero shifts $t \in [1, k-1]$, meaning shifted threads search for non-isomorphic permutations rather than $\pi$.
Count: 1

## Entries
| # | Original File | One-line Summary |
|---|---------------|------------------|
| 1 | explorer_r2_L00_N03 | Cyclic shifts $\pi(j) + t \pmod k$ strictly invert the relative vertical order of the extreme value pair $(\pi^{-1}(k), \pi^{-1}(1))$ for 100% of target permutations and all non-zero shifts $t \in [1, k-1]$ (0/18200 isomorphic matches), refuting multi-thread amplification. |
