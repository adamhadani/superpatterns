# Category: Discrete Buffer Integer Granularity

Type: failed_approach_category
Pattern: Continuous buffer scaling yields unphysical fractional host points ($< 1$) per interface, whereas discrete rank-slot separation requires $\ge 1$ integer host point per interface, incurring an $\Omega(2^j)$ penalty at scale $j$ and $\Omega(k)$ across fine scales, overwhelming $2\varepsilon k$ surplus.
Count: 56

## Entries
| # | Original File | One-line Summary |
|---|---------------|------------------|
| 1 | explorer_L00_N04 | Buffer separation $\Delta \ge 1$ across $2^j$ interfaces incurs $\ge 2^j \Delta$ penalty, yielding fine-scale loss $\ge k/2$ that strictly exceeds $2\varepsilon k$ surplus for all $\varepsilon < 1/4$. |
| 2 | explorer_L00_N00 | Claimed fractional buffer $\delta_J = O(k^{-1/2})$ per interface violates integer point capacity, while discrete rank separation requires $\ge 1$ point, forcing fine penalty $\Omega(k)$. |
| 3 | explorer_L00_N08 | Unphysical fractional buffer $\delta_j \to 0$ vs discrete requirement of $\ge 1$ point per interface; integer points require penalty $\ge 2^J \ge k$, wiping out surplus. |
| 4 | explorer_L00_N09 | Discrete atomicity violation: continuous buffer $\delta_j < 1/k$ is physically impossible in discrete permutations where each point occupies integer coordinates. |
| 5 | explorer_L00_N10 | Claimed buffer loss $O(2^{-3j/2}k)$, but discrete integer rank buffer forces $\sum 2^j \Delta = \Theta(k)$, exceeding $2\varepsilon k$ surplus by $\ge 50\times$. |
| 6 | explorer_L00_N12 | Fractional rank buffer $\delta_J = O(k^{-1/2}) < 1$ slot vs discrete requirement $\ge 1$ slot per interface, forcing fine-scale loss $\ge k/2$. |
| 7 | explorer_L00_N13 | Discrete buffer $\Delta 2^j = \Omega(2^j)$ yields total fine penalty $\ge 3k$ points, exceeding $2\varepsilon k$ surplus by $\ge 150\times$. |
| 8 | explorer_L00_N14 | Conflated Poisson standard deviation $O(2^{-j/2}k)$ with deterministic discrete rank buffer requirement $\Omega(2^j)$. |
| 9 | explorer_L00_N15 | Fractional buffer $\delta_J < 1$ slot violates integer point granularity; enforcing $\ge 1$ slot per interface forces penalty $\ge 2^J = \Theta(k)$. |
| 10 | explorer_L00_N16 | Lookahead rank boxes of area $\Theta(1/k^2)$ suffer from independent Poisson voids, with joint occupancy probability $\le \exp(-3.5k) \to 0$. |
| 11 | explorer_L00_N18 | Fractional buffer loss $C_{\mathrm{disc}}/\sqrt{k} \to 0$ violates discrete rank integrality; reserving $\ge 1$ point per interface forces $\Omega(k)$ penalty, wiping out surplus. |
| 12 | explorer_L00_N19 | Buffer scaling assumes fractional point loss $1/\sqrt{k} < 1$, but discrete rank buffers require $\ge 1$ integer point, forcing fine-scale penalty $P_J \ge k$. |
| 13 | explorer_L00_N20 | Boundary fluctuation scaling under Tracy-Widom grows as $k^{1/3} 2^{2j/3}$ rather than decaying, accumulating to $\Theta(k)$; discrete buffers require $\ge ck$ points. |
| 14 | explorer_L00_N21 | Buffer width scaling $\delta_j = C_1 2^{-3j/2} \ge 2/M$ requires $C_1 = \Omega(\sqrt{k})$, forcing $\delta_1 > 1$; discrete boundary slots sum to $\sum 2^j = 2k-2$, wiping out surplus. |
| 15 | explorer_L00_N22 | Per-interface penalty $p_j = O(2^{-3j/2} k) \to 0$ violates discrete rank integrality; reserving $\ge 1$ slot per interface requires $\ge 2k$ points. |
| 16 | explorer_L00_N23 | Wrong-sided bound $2^{-j/4} \le 2^{-j/2}$ in penalty derivation; expected points in fine buffers decay to $\Theta(k^{-1/2}) \to 0$, causing Poisson void starvation. |
| 17 | explorer_L00_N24 | Buffer width $\delta_J = c_0 k^{-3/2}$ yields $k \delta_J \approx k^{-1/2} < 1$ slot, violating discrete rank integrality and leaving buffer boxes 100% empty. |
| 18 | explorer_L00_N25 | Continuous boundary penalties assume fractional point loss, but discrete rank-slot separation requires $\ge 1$ point per interface, incurring $\ge k$ fine penalty. |
| 19 | explorer_L00_N26 | 100 disjoint rank boxes of size $1/k^2$ are 75% empty under Poisson intensity $(1/4+3\varepsilon/4)k^2$; discrete buffers require integer points, wiping out surplus. |
| 20 | explorer_L00_N27 | Rank-slot buffer spacing $2/(3k)$ across $k$ interfaces consumes $2/3$ (66.7%) of the unit interval, leaving negative net drift $-0.57$ at $\varepsilon=0.05$. |
| 21 | explorer_L00_N28 | Fractional buffer scaling violates discrete rank integrality; boundary penalty proof fabricated with undefined variable and trivial collision check. |
| 22 | explorer_L00_N29 | Inverted algebraic step dividing by $h_{j,m}$ causes actual discrete boundary loss to scale as $\Omega(2^{+j/2} k)$, diverging exponentially by $1024\times$ at $j=10$. |
| 23 | explorer_L00_N30 | Fractional buffer width $O(k^{-1/2})$ violates discrete integer rank slots; reserving $\ge 1$ slot per interface requires $\ge k$ points, exceeding $O(\varepsilon k)$ surplus. |
| 24 | explorer_L00_N31 | Discrete rank-slot buffer separation requires $\ge 1$ cell per interface, forcing penalty $\ge k$ on $k$ singletons and leaving negative net drift $D_{\mathrm{net}} \le (2\varepsilon - 1)k < 0$ for all $\varepsilon < 0.5$. |
| 25 | explore_merger_L01_N00 | Dilworth chain decomposition on residual $R$ claimed buffer penalties paid only at $d \le L$ endpoints ($P_{\mathrm{res}} = o(k)$); falsified because interleaved chains transition $\Theta(k)$ times with $>96\%$ adjacent switching, requiring $\ge 0.5k$ discrete buffer slots. |
| 26 | explore_merger_L01_N01 | Truncated dyadic chaining at mesoscopic scale $a_{\min}(\varepsilon) = \Theta(1/\varepsilon)$ to restrict boundary penalties, but ignored that mesoscopic blocks contain $k/2$ internal descents requiring $\ge 0.5k$ discrete buffer slots that overwhelm surplus. |
| 27 | explore_merger_L01_N02 | Truncated dyadic cuts at $j^*(\varepsilon)$ claiming $P_{\mathrm{coarse}} = O_\varepsilon(1)$, but reserved $2/M$ buffer between all $k$ target elements, consuming $33\%$ of coordinate space ($\approx 0.333k$ discrete slots) and driving net drift deeply negative. |
| 28 | explore_merger_L01_N03 | Required each residual element to occupy a rigid product box $X(t) \times Y(\pi(t))$ of expected count $\mu < 1/4$, failing with Poisson void starvation probability $\ge 1 - 10^{-93}$ at $k=100$. |
| 29 | explore_merger_L01_N08 | Claimed discrete buffer separation is paid only $m \le L$ times across Dilworth chains ($P_{\mathrm{res}} = o(k)$), whereas interleaved chains transition $\Theta(k)$ times ($96\%$ in random, $100\%$ in alternating), consuming $\Theta(k)$ discrete buffer slots and driving net drift to $-907.6$. |
| 30 | explore_merger_L01_N09 | Dyadic truncation defers but does not eliminate discrete integer buffer loss, requiring $\ge 1$ point per interface across discrete singletons and consuming $\ge k$ points. |
| 31 | explore_merger_L01_N11 | Residual sub-chain budgeting requires average run length $\ge 4/\varepsilon$ ($m_{\mathrm{res}} \le \varepsilon k / 4$), failing on generic (run length $2.42$) and alternating (run length $2.00$) permutations ($\le 10^{-155}$ of $S_{100}$); Dilworth chains switch $k-1$ times incurring $\Theta(k)$ buffer slots. |
| 32 | explore_merger_L01_N12 | Flexible rank-slot windows $W_t^{\mathrm{flex}}$ of area $\le 1/k^2$ have Poisson mean $\le 0.275$ and are $>76\%$ empty, with joint non-emptiness failing with probability $\ge 1 - 10^{-63}$ at $k=100$. |
| 33 | explore_merger_L01_N15 | Residual component $\mathcal{R}$ has $\ge k/2$ descent boundaries whose discrete buffer spacing consumes $\ge k/2$ points, driving effective velocity to $v_{\mathrm{eff}} \le 0.73 < 1.0$ at $\varepsilon=0.05$. |
| 34 | strategy_L02_N00 | Reserving discrete buffer separation $\ge 1$ point per interface on discrete rank slots incurs a total loss $\ge k/2$, wiping out the $(1/4+\varepsilon)k^2$ surplus. |
| 35 | strategy_L02_N02 | Discrete rank-slot isolation requires $\ge 1$ host point per interface across discrete singletons, yielding net negative drift across scales $j \le j^*$. |
| 36 | strategy_L02_N06 | Reserving boundary guard corridors shrinks mesoscopic host squares of size $a$, causing capacity deficit $c_{\mathrm{eff}} \le 0$ for $a \le 21.5$, which fails at threshold $L = \lceil K\sqrt{\log k}\rceil = 7$. |
| 37 | strategy_L03_N00 | Renewal drift under discrete integer rank-slot buffers incurs penalty $P_{\mathrm{disc}} \ge 0.413k$ (random) to $0.500k$ (alternating), strictly overwhelming gross continuous surplus $\le 0.0488k$ at $\varepsilon=0.05$ and driving net drift negative ($D_{\mathrm{net}} \le -0.3645k < 0$). |
| 38 | strategy_L03_N01 | Discrete integer buffer spacing incurs unavoidable penalty $P_{\mathrm{disc}} \ge 0.413k$, strictly overwhelming continuous surplus $\le 0.0488k$ at $\varepsilon=0.05$ and forcing net drift $D_{\mathrm{net}} \le -0.3642k < 0$ (requiring $\varepsilon > 0.498$ for positive drift). |
| 39 | strategy_L03_N02 | Shrunken candidate squares with guard corridors of width $\Delta/k$ suffer a capacity deficit at mesoscopic threshold $L = \lceil K\sqrt{\log k}\rceil$ (capacity $7.34 < 8$ at $k=100, \varepsilon=0.05, \Delta=1$), requiring block size $a \ge 21.49 \gg L$. |
| 40 | strategy_L03_N04 | Discrete buffer losses $\ge 0.5k$ strictly overwhelm continuous surplus $\le 0.0954k$ at $\varepsilon=0.05$, driving net drift to $D_{\mathrm{net}} \le -0.4046k < 0$ and effective velocity to $v_{\mathrm{eff}} \le 0.7303 < 1.0$. |
| 41 | strategy_L03_N04 | Theorem 5.1 dilemma: defining constant block size $a_{\min} = \Theta(1/\varepsilon)$ causes union bound over $O(k^3)$ candidate squares to diverge as $\Theta(k^3) \to +\infty$, while setting $a \ge K\sqrt{\log k}$ causes a strict capacity deficit ($\sqrt{1+2\varepsilon}(a-\Delta) < a$) for all $k \le 10000$. |
| 42 | strategy_L04_N00 | Self-refuting primary route: discrete integer rank-slot separation incurs buffer penalty $P_{\mathrm{disc}} \ge 0.413k$ (random) to $0.500k$ (alternating), strictly overwhelming gross continuous surplus $\le 0.0488k$ at $\varepsilon=0.05$ by $8\times$ to $10\times$ and driving net traversal drift to $D_{\mathrm{net}} \le -0.3645k < 0$ with subcritical velocity $v_{\mathrm{eff}} \le 0.7303 < 1.0$. |
| 43 | explorer_r3_L00_N01 | Macroscopic buffer amortization $2M_0 = \Theta(1)$ fails because quasirandom target permutations cross cell boundaries $\Theta(k)$ times ($99.4\%$ boundary transitions), requiring discrete buffers at each boundary and driving buffer deficit to $\ge 0.5k$. |
| 44 | explorer_r3_L00_N05 | Buffering only column boundaries ignores vertical interfaces; enforcing full coordinate separation across both axes requires $\ge k/2$ buffer points, driving effective velocity to $v_{\mathrm{eff}} \le 0.7303 < 1.0$. |
| 45 | explorer_r3_L00_N11 | Dyadic buffer truncation at scale $j^*$ ignores 484 internal descents inside 32 blocks, costing $\ge 484$ buffer points that overwhelm continuous surplus by $5\times$ (net drift $\le -384$). |
| 46 | explorer_r3_L00_N13 | Partitioning into $m \le 2\varepsilon k$ blocks of length $\ge 1/(2\varepsilon)$ fails: random permutations have average run length 2.42, requiring $\ge 0.413k$ blocks whose discrete buffers force net drift $D_{\mathrm{net}} \le -0.313k < 0$. |
| 47 | N19 (Level 0) | Domain blocks $B_r$ do not partition values in generic simple permutations (>96% vertical overlap), requiring $k-1$ discrete buffers costing $P_{\mathrm{buffer}} \ge 0.413k$ |
| 48 | N23 (Level 0) | Mesoscopic cut termination at $j^*(arepsilon)$ ignores internal descents and vertical spreading, re-imposing discrete buffers |
| 49 | N30 (Level 0) | Buffer amortization $P_{\mathrm{disc}} \le d\cdot\Delta$ confuses chain count $d$ with domain chain transitions; random permutations switch chains on $>85\%$ of consecutive steps, forcing buffer losses to scale as $\Theta(k)\Delta \ge 0.85 k \gg 2\varepsilon k$ |
| 50 | explore_merger_r3_L01_N02 | Dyadic boundary truncation at scale $j^*$ leaves inter-block cuts but ignores $\sim k/2$ internal descents across blocks, which consume $\ge 0.5k$ discrete buffer slots and overwhelm continuous surplus ($2\varepsilon k$) by $5\times$ to $10\times$ ($D_{\mathrm{net}} \le -0.40k < 0$). |
| 51 | explore_merger_r3_L01_N03 | Generic permutations average monotone run length $1.50$ with $\sim 0.500k$ descents; discrete rank buffering at internal descents consumes $\ge 0.500k$ points, driving net renewal drift to $D_{\mathrm{net}} \le -0.400k < 0$ at $\varepsilon=0.05$. |
| 52 | explore_merger_r3_L01_N07 | Truncating dyadic chaining at $j^*(\varepsilon)$ leaves 128 terminal blocks of size $\Theta(\varepsilon k)$ where internal discrete integer buffering consumes $\ge 0.413k$ points, overwhelming continuous surplus ($0.0488k$ at $\varepsilon=0.05$) and yielding net drift $-364.19$ at $k=1000$ (positive net drift requires $\varepsilon > 0.4983$). |
| 53 | explore_merger_r3_L01_N14 | Coarse dyadic interface buffering fallacy: at $\varepsilon = 0.05$ ($j^* = 12$), the $\sum_{j=1}^{12} (2^j - 1) = 8,178$ coarse interfaces require $16,356$ buffer points (with $\Delta=2$), which overwhelms the continuous surplus $2\varepsilon k = 100$ at $k=1000$ by $163.5\times$ (and by $16.35\times$ at $k=10,000$). |
| 54 | explore_merger_r3_L01_N13 | Algebraic exponent error in dyadic boundary buffering ($k^2 \cdot k^{-1/2} = k^{1.5} \ne k$) causes boundary penalty $P_j = \Theta(k^{3/2}) \gg 2\varepsilon k$, while 2D grid perimeter growth $\Theta(2^j)$ forces buffer losses to grow as $\Theta(2^j k)$ points. |
| 55 | explore_merger_r3_L02_N00 / N07 | Truncating dyadic chaining at $j^*(\varepsilon)$ leaves $\Theta(\varepsilon k)$ terminal blocks requiring $\ge 0.413k$ discrete integer buffer points, incurring a net buffer deficit of $-364.49$ at $k=1000$ and $\varepsilon=0.05$ (continuous surplus is only $0.0488k$, positive net drift requires $\varepsilon > 0.4983$). |
| 56 | explore_merger_r3_L03_N02 | Discrete integer rank buffers require $\ge 1$ host point per descent interface across the $\approx 0.413k$ to $0.499k$ descents in generic permutations, consuming $P_{\mathrm{buf}} \ge 0.413k$ to $0.499k$ points and swamping continuous surplus ($0.0488k$ at $\varepsilon=0.05$) by $8.5\times$ to $10\times$, forcing net traversal drift deeply negative ($D_{\mathrm{net}} \le -0.3645k < 0$; positive drift strictly requires $\varepsilon > 0.4987$). |
