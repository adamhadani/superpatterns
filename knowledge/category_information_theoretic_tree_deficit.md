# Category: Information-Theoretic Tree Deficit

Type: failed_approach_category
Pattern: A dyadic chaining tree of description entropy $O(\varepsilon^2 k)$ or $O(k)$ cannot embed $S_k$, because Shannon source coding requires $\log_2(k!) = \Theta(k \log k)$ bits to represent an arbitrary permutation, leaving the tree covering less than a $2^{-\Omega(k \log k)}$ fraction of targets.
Count: 64

## Entries
| # | Original File | One-line Summary |
|---|---------------|------------------|
| 1 | explorer_L00_N02 | Claimed a tree with entropy $O(\varepsilon^2 k)$ covers all $k!$ permutations; provides $< 0.1$ bits at $k=1000, \varepsilon=0.01$ vs 8529.4 bits required, covering $< 2^{-8500}$ of $S_k$. |
| 2 | explorer_L00_N04 | Conflated single-interval entropy with total tree entropy by omitting factor $2^j$, undercounting actual tree entropy $\Theta(k \log k)$ by over $100\times$. |
| 3 | explorer_L00_N10 | Claimed coarse configurations are bounded by $(2^{j^*})!$, whereas in reality interval images disperse across bins giving $(2^{j^*})^k = \exp(\Theta(k \log(1/\varepsilon)))$. |
| 4 | explorer_L00_N12 | Assumed description entropy $O(k)$ suffices to cover $S_k$, whereas Shannon source coding requires $\Theta(k \log k)$ bits, leaving an exponential information deficit. |
| 5 | explorer_L00_N16 | Tree description entropy $O(k)$ bits cannot encode $k! = \exp(\Theta(k \log k))$ permutations; individual sparse paths cannot induce super-exponentially many distinct patterns. |
| 6 | explorer_L00_N20 | Coarse trajectory space of size $C_0(\varepsilon)$ forces each coarse path to be shared by $k!/C_0(\varepsilon) = \exp(\Theta(k \log k))$ distinct permutations, leaving fine-scale permutations uncertified. |
| 7 | explorer_L00_N21 | Claimed dyadic tree profile with $\le 3$ choices per node can describe at most $e^{O(k)}$ permutations, omitting $99.99999\%$ of $S_k$ targets. |
| 8 | explorer_L00_N23 | Exact dyadic tree value-split telescoping product $\prod_{j=0}^{\log_2 k - 1} \binom{2k/2^j}{k/2^j}^{2^j/2} = k!$, proving tree entropy is $\Theta(k \log k)$ bits, not $O(k)$. |
| 9 | explorer_L00_N24 | Chaining tree with description entropy $O(k)$ bits cannot certify $k! = \exp(\Theta(k \log k))$ permutations, failing to cover all but a negligible fraction of targets. |
| 10 | explorer_L00_N27 | Dyadic trajectory tree with $O(k)$ description entropy violates Shannon entropy bound $\log_2(k!) = \Theta(k \log k)$ by a factor of $\log k$. |
| 11 | explorer_L00_N28 | Dyadic bounding box trees distinguish all $k!$ permutations (combinatorially enumerated on $S_7, S_8$), proving tree entropy is $\Theta(k \log k)$ bits, not $O(k)$. |
| 12 | explorer_L00_N30 | Inverted scale dependence in tree entropy ($2^{-j} k$ instead of $2^j$), claiming all $k$ permutation values are specified in $O(1)$ bits vs Shannon $\Theta(k \log k)$. |
| 13 | explorer_L00_N31 | Fixed point set induces only one permutation, so $e^{O(k)}$ paths cannot certify $k!$ targets, leaving an exponential information deficit. |
| 14 | explore_merger_L01_N10 | Claimed dyadic tree description entropy $O(\varepsilon^2 k)$ covers $S_k$, violating Shannon source coding bound $\log_2(k!) = \Theta(k \log k)$ by over $8528$ bits at $k=1000$ (covering $\le 2^{-8528}$ of $S_k$). |
| 15 | explore_merger_L01_N14 | Truncation at $j^*(\varepsilon)$ leaves $\exp(\Theta(k \log k))$ microscopic permutations per coarse profile uncertified; fine-scale union bound explodes with exponent $+k \ln k - c_1 \varepsilon^2 k \approx +277$ at $k=100$. |
| 16 | strategy_L02_N01 | Shannon source coding lower bound $\log_2(k!) = \Theta(k \log k)$ proves $O(k)$ dyadic trees cannot cover $S_k$. |
| 17 | strategy_L02_N02 | Dyadic tree coding capacity $O(\varepsilon^2 k) \le 10$ bits falls short of $\log_2(k!) \approx 524$ bits at $k=100$. |
| 18 | strategy_L02_N07 | Shannon information deficit: tree decomposition code with $O(k)$ depth cannot represent $k!$ permutations without factorial branch complexity. |
| 19 | strategy_L03_N00 | Candidate reduction line 6 asserts that a single common host event simultaneously embeds all $k!$ permutations with failure probability $\exp(-\Omega(\varepsilon^2 k))$, directly contradicting Shannon source coding requirement of $\Theta(k \log k)$ bits and causing union bound net exponent $\ln(k!) - c\varepsilon^2 k \approx +5910.9$ nats to explode at $k=1000$. |
| 20 | strategy_L03_N01 | Candidate reduction line 6 claims simultaneous containment with $o(1)$ error over all $k!$ permutations, contradicted by the $\Theta(k \log k)$ Shannon information deficit producing net union bound divergence of $+5909.6$ nats at $k=1000$ (deficit ratio $>2360\times$). |
| 21 | strategy_L03_N04 | Candidate reduction line 6 asserts simultaneous containment over $S_k$ with failure probability $o(1)$, contradicted by the $\Theta(k \log k)$ Shannon information deficit where net union bound exponent $\ln(k!) - c\varepsilon^2 k \approx +5911.8$ nats explodes by $10^{2567}$ at $k=1000$. |
| 22 | strategy_L04_N00 | Active Obligation 02 posits a common host event on polynomial structures bypassing the $\Theta(k \log k)$ Shannon deficit for the residual sector; unconditioned union bound exponent $\ln(k!) - c\varepsilon^2 k \approx +5910.9$ nats diverges by over $4700\times$ at $k=1000$, and local surplus ($0.0488$) is dwarfed by interface entropy ($7.55$) by $150\times$. |
| 23 | explorer_r2_L00_N04 | Interleaving $M = O(\varepsilon k)$ overlapping mesoscopic blocks of length $L$ into a target permutation of length $k = M L$ requires $\ln(k! / (L!)^M) \approx 2628$ nats of ordering entropy at $k=1000$, which strictly overwhelms the discrete buffer budget of $(1/4)\varepsilon k = 12.5$ points by over $210$ nats per buffer point. |
| 24 | explorer_r2_L00_N08 | Hypergraph container certificate defined over edges order-isomorphic to 'at least one simple permutation' commits an existential-universal quantifier shift, failing to certify containment of all $(1-o(1))k!$ simple permutations against the $\Theta(k \log k)$ Shannon factorial deficit. |
| 25 | explorer_r2_L00_N12 | Bufferless Janson matching has linear Janson exponent $\Theta(k)$ rather than quadratic $\Omega(k^2)$, so the union bound over all $k!$ permutations diverges by $+5412$ nats at $k=1000$ ($\ln(k!) - \Theta(k) \approx +5910 - 498 > 0$). |
| 26 | explorer_r2_L00_N13 | Tree representation with linear description entropy cannot cover $k!$ permutations, failing to overcome the Shannon source coding lower bound $\Theta(k \log k)$. |
| 27 | explorer_r2_L00_N19 | Structured-map description entropy ($1932.6$ nats at $k=1000$) exceeds single-path Poisson concentration margin ($1.25$ nats) by $>1500\times$, producing a net union bound divergence of $+1931.4$ nats, while multi-thread lookahead branching tree entropy $k \ln \Delta \ge 1.10k$ exceeds gross surplus $0.0488k$ by $>22\times$. |
| 28 | explorer_r2_L00_N20 | Fabricated $\log k$ exponent boost in single-path structured map concentration $\exp(-\Omega(\varepsilon^2 k \log k))$, whereas Poisson large deviations are strictly linear $\exp(-\Omega(\varepsilon^2 k))$, leaving the union bound over $\exp(O(k \log\log k))$ structured maps diverging by $+1928.0$ nats at $k=1000$. |
| 29 | explorer_r2_L00_N24 | Claimed a deterministic family of $e^{O(k)}$ multi-line skeleton templates can certify simultaneous containment of all $k!$ target permutations, violating the Shannon source coding lower bound $\log_2(k!) = \Theta(k \log k)$ bits by over $4700\times$ and leaving unconditioned union bounds diverging by $+5910.9$ nats at $k=1000$. |
| 30 | strategy_L01_N01 | Catalog $\mathcal{Z}_k$ description entropy $21 k \ln\ln k = 40,585$ nats at $k=1000$ strictly exceeds single-path Poisson concentration margin $1.25$ nats by $+40,584$ nats, exploding the simultaneous union bound. |
| 31 | strategy_L01_N09 | Structured map catalog $\mathcal{Z}_k$ description entropy $21 k \ln\ln k$ exceeds Poisson concentration margin by $+40,584$ nats at $k=1000$; single-target containment at $C=2.0$ has Chernoff exponent $184$ nats, dwarfed by $\ln(k!) \approx 5912$ nats by $+5728$ nats. |
| 32 | strategy_L01_N12 | Encoding all $k!$ permutations requires $\Theta(k \log k)$ bits by Shannon source coding; dyadic tree catalog with sub-factorial entropy cannot certify universality. |
| 33 | strategy_r2_L02_N00 | Reduces simultaneous universality to an unconstructed common host certificate digraph or host-side branching order tree with failure probability $\exp(-\Omega(\varepsilon^2 k^2))$, but distinguishing $k!$ permutations requires $\Theta(k \log k)$ bits ($+5910.9$ nats deficit at $k=1000$), while Poisson cell intensity $\mu \le 0.275 < 1.0$ forces 100% extinction of host branching trees. |
| 34 | strategy_r2_L02_N01 | Posits host certification events $E_{\mathrm{host}}$ on $\Pi_{n_0}$ of size $\le \exp(O(\varepsilon^2 k))$ to bypass $\ln(k!)$, but critical cell intensity $\mu_{\mathrm{cell}} \le 0.300 < 1.0$ forces branching extinction, and structured map catalog entropy $21 k \ln(\ln k)$ ($40,585.5$ nats at $k=1000$) leaves a $+40,581.4$ nats deficit. |
| 35 | strategy_r2_L02_N02 | Risky Lemma 3 claims structured maps $\mathcal{Z}_k$ embed with failure probability $\le \exp(-\Omega(k \log^2 k))$, but structured maps cannot use multi-threaded shift decorrelation, leaving a $+40,584.3$ nats union bound divergence against linear Poisson concentration ($1.25$ nats at $k=1000$). |
| 36 | strategy_r2_L02_N03 | Backup Route 1 claims to prove quadratic universality at $C \in (1.0, 2.5]$ from single-target renewal failure, but the union bound across $k!$ targets diverges by $+5462.1$ nats at $k=1000, C=2.5$, conflating single-target containment with simultaneous universality. |
| 37 | strategy_r2_L02_N04 | Asserts unconditional universality at $C=2.5$ via Theorem 6.1, but drops child falser objections on structured maps $\mathcal{Z}_k$ whose catalog entropy ($21 k \ln\ln k = 40,567.9$ nats at $k=1000$) diverges against single-path Poisson concentration by $+40,567.9$ nats (ratio $2,295\times$). |
| 38 | strategy_r2_L02_N05 | Posits a common host certificate to bypass $\ln(k!)$, but distinguishing $k!$ permutations requires $\Theta(k \log k)$ bits ($+5908.0$ nats deficit against Poisson concentration at $k=1000$), requiring an unbridgeable $10^{2565.8}\times$ multiplexing factor. |
| 39 | strategy_r2_L02_N07 | Lemma 5 posits a common host event on $\Pi_{n_0}$ with failure probability $\le \exp(-\Omega(\varepsilon^2 k^2))$, but provides no construction against the Shannon source coding deficit of $+5910.88$ nats at $k=1000$. |
| 40 | strategy_r2_L03_N00 | Common host certificate $E_{\mathrm{host}}$ on $\Pi_{n_0}$ of size $\le \exp(O(\varepsilon^2 k))$ (Risky Lemma 5) fails because $\ln(k!)$ deficit is $+5911.09$ nats at $k=1000$, and lookahead cells at $C = 0.275$ have $\mu_{\mathrm{cell}} = 0.1222$ (88.5% void), extinguishing branching trees (survival $5.45 \times 10^{-42}$ over 100 steps). |
| 41 | strategy_r2_L03_N01 | Single realization event $E_{\mathrm{host}}$ (Lemma 5) simultaneously embedding all quasirandom targets cannot overcome $+5910.9$ nats Shannon deficit, while lookahead cells collapse to $\mu = 0.122$ with 61.3% joint 4-cell void probability per step. |
| 42 | strategy_r2_L03_N02 | Positing common host event $E_{\mathrm{host}}$ of size $\le \exp(O(\varepsilon^2 k))$ (Risky Lemma 5) violates Shannon limit by $+5911.09$ nats at $k=1000$ and suffers 100% path extinction in subcritical Poisson cells. |
| 43 | strategy_r2_L03_N03 | Claiming a single common host event $E_{\mathrm{host}}$ bypasses the factorial deficit (Risky Lemma 5) ignores $+5907.96$ nats gap and 100% branching tree extinction under cell density $\mu_{\mathrm{cell}} \le 0.122$. |
| 44 | strategy_r2_L03_N04 | Asserting a common host certificate digraph or order-tree on $\Pi_{n_0}$ (Risky Lemma 6) fails by $+5907.96$ nats at $k=1000$, while cell mean $\mu_{\mathrm{cell}} = 0.122 < 1$ causes 100% branching extinction. |
| 45 | strategy_r2_L04_N00 | Risky Lemma 5 posits a common host certificate of size $\le \exp(O(\varepsilon^2 k))$ to bypass $\ln(k!)$, but distinguishing $k!$ targets requires $\Theta(k \log k)$ bits ($+5911.09$ nats deficit at $k=1000$), while Poisson cell intensity at $C=0.275$ collapses to $\mu_{\mathrm{cell}} = 0.1222$ (88.50% empty) with 61.33% 4-cell void probability per step, causing 100% branching extinction (100-step path survival $5.45 \times 10^{-42}$). |
| 46 | explorer_r3_L00_N00 | Dyadic tree encoding claims linear description entropy by dropping the $2^j$ interval multiplicity factor, undercounting actual tree entropy $\Theta(k \log k)$ bits by over $100\times$. |
| 47 | explorer_r3_L00_N04 | Encoding $k!$ permutations into $O(k^4)$ macroscopic corridors provides capacity at most $\exp(3607)$ nats vs $\ln(1000!) = 5912$ nats, leaving an unbridgeable information deficit of $+2305$ nats. |
| 48 | explorer_r3_L00_N06 | Mesoscopic occupancy provides macroscopic positioning but zero fine-scale ranking bits, leaving the full $\ln(1000!) \approx 5912$ nats deficit unaddressed against linear Poisson concentration. |
| 49 | explorer_r3_L00_N10 | Union bound over $\le \exp(O(\varepsilon^2 k))$ containers commits an existential-universal quantifier shift, while each container holds $\ge \exp(k \ln k)$ permutations (+5909.6 nats deficit at $k=1000$). |
| 50 | explorer_r3_L00_N11 | Applying Deuschel-Zeitouni lower-tail bounds $\exp(-\Omega(k^2))$ to non-monotone dyadic blocks is a category error; true renewal rate is linear $\exp(-\Omega(\varepsilon^2 k))$, leaving +5910.9 nats deficit. |
| 51 | explorer_r3_L00_N12 | Mesoscopic block entropy $(k/M)\ln(M!) = O_\varepsilon(k)$ accounts only for intra-block permutations and omits multinomial cross-block value assignment entropy $\Theta(k \ln k)$ (+1979.2 nats at $k=1000$). |
| 52 | N16 (Level 0) | Coarse envelope description entropy $O_arepsilon(1)$ drops fine-scale $\Theta(k \ln k)$ target entropy per envelope (+5911 nats deficit at $k=1000$) |
| 53 | N19 (Level 0) | Macroscopic profile union bound drops cross-block interleaving entropy $\Theta(k \ln k)$ (+5686.4 nats deficit at $k=5000$) |
| 54 | N21 (Level 0) | Shannon entropy deficit $\Theta(k \log k)$ cannot be covered by linear Poisson area margin; cell intensity collapses to $\mu=0.1222$ |
| 55 | N22 (Level 0) | Cluster assignment path entropy $2k \ln M pprox 5051$ nats exceeds Poisson margin (2.50 nats) by 2020x; false claim $arepsilon k \log k \le O(arepsilon k \log(1/arepsilon))$ |
| 56 | N23 (Level 0) | Shannon deficit (+5877 nats at $k=1000$) inverts simultaneous ribbon certificate |
| 57 | N26 (Level 0) | Macro-profile grouping only buckets points; intra-box micro-permutations retain $(1 - O(\varepsilon))k\ln k$ nats of entropy, causing union bound over intra-box configurations to diverge to $+\infty$ |
| 58 | N28 (Level 0) | Common host certificate $E_{\mathrm{cert}}$ asserts only coarse corridor point density, leaving the $\Theta(k\ln k)$ Shannon factorial deficit completely unbridged |
| 59 | N30 (Level 0) | Macroscopic host certificate $E_{\mathrm{host}}$ on $O_\varepsilon(1)$ boxes holds even at subcritical intensity $C = 0.20 < 1/4$ where $\operatorname{id}_k$ cannot be embedded, providing zero guarantee of fine-scale embedding |
| 60 | explore_merger_r3_L01_N01 | Mesoscopic block description entropy claims $(k/M)\ln(M!) = O_\varepsilon(k)$ but drops cross-block multinomial value assignment entropy $\ln(k! / (M!)^{k/M}) = \Theta(k \ln k)$, covering less than $10^{-859}$ of $S_{1000}$. |
| 61 | explore_merger_r3_L01_N09 | Coarse dyadic envelope entropy $O_\varepsilon(1)$ merely conceals the $+5291$ nats Shannon factorial deficit (at $k=1000$) inside terminal blocks of size $m = \Theta(\varepsilon k)$, which cannot be multiplexed onto shared corridors under subcritical Poisson vacancy. |
| 62 | explore_merger_r3_L01_N10 | Coarse mesoscopic corridor certificate requires description entropy $O(k^{1/3}\ln k) \ll \Theta(k\ln k)$, leaving an unbridged Shannon deficit of $+5908$ nats at $k=1000$, while common host certificates of size $\exp(O(\varepsilon^2 k))$ commit an existential-universal quantifier shift averaging $\ge 10^{2566}$ distinct permutations per event. |
| 63 | explore_merger_r3_L03_N03 | Distinguishing $k!$ target permutations strictly requires $\ln(k!) = \Theta(k \ln k)$ nats ($5912.13$ nats at $k=1000$), while single-path Poisson concentration margin at intensity $(1/4+\varepsilon/2)k^2$ is only $1.25$ nats, leaving an unbridgeable net deficit of $+5910.88$ nats, while coarse envelopes multiplex $\sim 10^{2297}$ fine-scale permutations without branching capability. |
| 64 | explore_merger_r3_L04_N00 | Distinguishing $k!$ target permutations requires $\ln(k!) = 5912.13$ nats at $k=1000$ leaving an unconditioned Shannon deficit of $+4870.46$ nats over Poisson concentration, while at $C = 0.275$, grid cells collapse to $\mu_{\mathrm{cell}} = 0.1222$ ($88.50\%$ empty cells) triggering $100\%$ Galton-Watson branching extinction (100-step survival $5.452 \times 10^{-42}$). |
