# Factorial Target Information Deficit in Sub-Factorial Trees

Type: observation
Confidence: high
Source: Candidates 0, 2, 4, 5, 6, 7, 16, 20, 21, 23 (Level 0 falsers); Candidates L03_N00, L03_N01, L03_N03, L03_N04 (Level 3 falsers); Candidate L04_N00 (Level 4 falser); explore_falser_r2_L03_N00, explore_falser_r2_L03_N01, explore_falser_r2_L03_N02, explore_falser_r2_L03_N03, explore_falser_r2_L03_N04 (Round 2 Level 3 falsers); explore_merger_r2_L04_N00, explore_falser_r2_L04_N00 (Round 2 Level 4); explore_falser_r3_L00_N12 (Round 3 Level 0); explore_merger_r3_L04_N00, explore_falser_r3_L04_N00 (Round 3 Level 4)
Relevant to: Description entropy, simultaneous universality, union bounds over permutations.

## Statement
By Shannon source coding and the pigeonhole principle, any discrete catalog or tree of embedding paths $\mathfrak{T}$ that certifies simultaneous containment of all $k!$ target permutations in $S_k$ must have description entropy of at least $\log_2(k!) = k \log_2(k/e) + O(\log k) = \Theta(k \log k)$ bits. Any tree structure restricted to entropy $O(\varepsilon^2 k)$ or $O(k)$ can represent at most $2^{O(k)}$ distinct permutation orderings, covering less than a $2^{-\Omega(k \log k)}$ fraction of $S_k$.

## Evidence
1. Exact information calculation: For $k=1000$, $\log_2(1000!) \approx 8529.4$ bits. A tree with claimed fine-scale entropy $(c/4)\varepsilon^2 k$ at $\varepsilon=0.01$ provides at most $0.10$ bits (an information deficit of $85,294\times$).
2. Candidate 23 falsification proved the exact telescoping identity for dyadic tree value partitions:
$$\prod_{j=0}^{\log_2 k - 1} \left[ \binom{2k/2^j}{k/2^j} \right]^{2^j/2} = k!$$
proving that dyadic tree partition entropy is identically $\ln(k!) = k \ln k - k + O(\log k)$, refuting the claim that scale $j$ contributes only $O(2^{-j} k)$ bits without multiplying by $2^j$.
3. Pigeonhole principle: If an interface profile $P$ uniquely determines the permutation's relative coordinate ordering, each profile embeds at most 1 permutation. Thus $|\mathfrak{T}| \ge k!$.
4. When authors claim $|\mathfrak{T}| \le \exp(O(k))$, they either omit the fine-scale permutations sharing coarse profiles (leaving $e^{\Omega(k)}$ fine paths uncertified) or omit the $2^j$ branching factor across dyadic tree levels.
5. Level 3 Candidate Reduction Internal Contradiction (Level 3 falsers L03_N00, L03_N01, L03_N04): Level 3 candidate reductions asserted that a single host event simultaneously embeds all $k!$ permutations with failure probability $\exp(-\Omega(\varepsilon^2 k))$, directly contradicting Shannon source coding. Taking an unconditioned union bound over individual target paths against linear concentration $\exp(-c\varepsilon^2 k)$ gives net exponent $\ln(k!) - c\varepsilon^2 k \approx k\ln(k/e) - c\varepsilon^2 k \to +\infty$ (at $k=1000, \varepsilon=0.05$, net exponent is $+5911.8$ nats, an information deficit exceeding $18,900\times$).
6. Insufficiency of Polynomial Dyadic Boxes for Fine-Scale Patterns (Level 3 falser L03_N03): Point count concentration across a polynomial family of dyadic host boxes $\mathcal{B}$ ($|\mathcal{B}| = O_\varepsilon(k^2)$) only certifies macroscopic Poisson densities ($|\Pi_{n_0} \cap B| \approx n_0 |B|$), which holds even at subcritical host intensities $C = 0.20 < 1/4$ where $\operatorname{id}_k$ cannot be embedded. It provides zero certificate for the existence of the specific fine-scale permutation pattern $\pi_{\mathrm{block}} \in S_{a_{\min}}$ within terminal blocks, where the description entropy rate $\ln(a_{\min}!)/a_{\min} \in [5.18, 7.48]$ nats/element exceeds the Poisson concentration margin $c\varepsilon^2 \le 0.00125$ by $1036\times$ to $149,000\times$.
7. Level 4 Falsification (Level 4 falser L04_N00): In the Level 4 Master Strategy Card, Active Obligation 02 posited that a common host event on polynomial or shared structures could bypass the $\Theta(k \log k)$ Shannon deficit for arbitrary residual permutations. Falsification confirmed that the gross forward drift surplus above unity at $C = 1/4+\varepsilon$ is at most $0.0488$ nats/element (at $\varepsilon=0.05$), which is dwarfed by the interface description entropy rate ($7.55$ nats/element) by over $150\times$, while the unconditioned union bound net exponent $\ln(k!) - c\varepsilon^2 k$ diverges by $+5910.9$ nats at $k=1000$ (deficit ratio $> 4700\times$).
8. **Structured Map Catalog Entropy Divergence and Lookahead Cell Void Extinction (Round 2 Level 3 falsers L03_N01, L03_N03, L03_N04)**:
In candidate L03_N03, the proposed catalog of structured maps $\mathcal{F}$ had cardinality $\approx 2^{120} \approx 1.3 \times 10^{36}$ (entropy $\approx 83.2$ nats). Against a target permutation space of $S_{5000}$ ($\ln(5000!) \approx 40,664$ nats), the catalog divergence is $+40,581$ nats. In candidate L03_N04 Backup Route 1, the deficit reached $+5328$ nats at $k=1000$.
Furthermore, when candidate L03_N00/N01/N02/N03 strategies attempted to compress the W47 quadratic baseline from $C_0 \approx 9.62$ down to $C = 1/4 + \varepsilon = 0.275$, the expected point count in lookahead cells collapsed from $\mu_{\mathrm{cell}} = 4.67$ down to $\mu_{\mathrm{cell}} = 0.122$ points/cell (a 38.3-fold density collapse). Consequently, an individual lookahead cell is empty with probability $e^{-0.122} = 88.5\%$, and a 4-cell lookahead step suffers a $61.3\%$ joint void probability per step. Across 100 sequential embedding steps, the survival probability of the lookahead branching tree decays as $(1 - 0.613)^{100} \approx 5.45 \times 10^{-42}$, causing total tree extinction and proving that lookahead branching cannot certificate simultaneous containment.
9. **Round 2 Level 4 Common Host Certificate Audit (Round 2 Level 4 falser L04_N00)**:
Auditing the Level 4 Master Strategy Card confirmed that the Shannon source coding deficit $\ln(k!) - \text{margin}$ diverges from $+363.64$ nats at $k=100$ to $+5911.09$ nats at $k=1000$ and $+37,585.94$ nats at $k=5000$. Because subcritical cell Poisson intensity ($\mu_{\mathrm{cell}} = 0.1222$) extinguishes lookahead trees (100-step path survival $5.45 \times 10^{-42}$), no common host certificate can bypass this deficit.
10. **Multinomial Cross-Block Value Assignment Entropy Barrier (Round 3 Level 0 falser L00_N12)**:
Mesoscopic block decompositions into $M$ blocks of size $k/M$ claim intra-block description entropy is $(k/M)\ln(M!) = O_\varepsilon(k)$. However, this accounts strictly for internal permutations within blocks and completely drops the multinomial cross-block value assignment entropy:
$$\ln \binom{k}{M, M, \dots, M} = \ln \left( \frac{k!}{(M!)^{k/M}} \right) = \Theta(k \ln k) \text{ nats}$$
For $k=1000$ and $M=50$, this omitted cross-block assignment entropy alone equals $+1979.2$ nats (covering less than $10^{-859}$ of $S_{1000}$). Local block compression cannot bypass the global cross-block value assignment complexity.
11. **Round 3 Level 4 Exact Shannon Deficit vs Poisson Margins (explore_merger_r3_L04_N00, explore_falser_r3_L04_N00)**:
    $\ln(1000!) = 5912.13$ nats vs Poisson Chernoff margin $1041.67$ nats, leaving an unconditioned Shannon deficit of $+4870.46$ nats (and $+5910.88$ nats vs single-path margin $1.25$ nats), while under subcritical Poisson cell density $\mu_{\mathrm{cell}} = 0.1222$ ($88.50\%$ empty cells), Galton-Watson offspring mean $m = 0.4602 < 1.0$ guarantees 100% lookahead branching tree extinction (100-step survival $5.452 \times 10^{-42}$).

## Implications
Simultaneous containment cannot be proved by taking a union bound over a tree of entropy $O(k)$ against a single-path concentration decay $\exp(-c \varepsilon^2 k)$. Either the tree does not cover $S_k$, or if the tree is expanded to cover $S_k$, the entropy $\Theta(k \log k)$ overwhelms the linear concentration exponent $\Omega(\varepsilon^2 k)$.

## Caveats
Shared host structures (such as shared squares $\mathcal{Q}_{\mathrm{squares}}$ of polynomial size $O(k^3)$) can be reused without multiplying entropy, but only for monotone blocks, not for unstructured arbitrary permutations.
