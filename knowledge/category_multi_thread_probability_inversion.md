# Category: Multi-Thread Probability Inversion

Type: failed_approach_category
Pattern: Confusing exponentially rare success with exponentially rare failure in multi-threaded scanning: because single-thread completion $x_k \le 1$ is an exponentially rare lower-tail event ($\le \exp(-I(C)k) \le 10^{-50}$ at $C \le 0.30$), single-thread failure is the overwhelmingly typical event ($1 - o(1) \approx 1.0$), so joint failure across $T = \log^2 k$ threads is $1 - o(1) \to 1.0$, completely refuting claimed super-factorial decay $\exp(-\Omega(k \log^2 k))$.
Count: 32

## Entries
| # | Original File | One-line Summary |
|---|---------------|------------------|
| 1 | explorer_r2_L00_N00 | Falsely claimed single-thread failure probability is $\exp(-c\varepsilon^2 k)$, whereas exact Poisson-gamma analysis proves failure occurs with probability $1 - \exp(-0.636k) \approx 1.0$, causing joint failure across $T = \log^2 k$ threads to approach $1.0 - o(1)$. |
| 2 | explorer_r2_L00_N02 | Inverted success and failure events at $C = 1/4+\varepsilon$: single-thread success probability is $\le \exp(-0.636k)$ and failure is $1 - \exp(-0.636k) \approx 1.0$, so multiplying failure probabilities across $T$ threads yields $1 - T\exp(-0.636k) \to 1.0$ rather than $\exp(-c k \log^2 k)$. |
| 3 | explorer_r2_L00_N05 | Asserted joint failure probability $\exp(-c_0 \varepsilon^2 k \log^2 k)$ via product conditioning, contradicting the Harris-FKG inequality for decreasing Poisson failure events and inverting single-thread failure probability ($\approx 1 - 10^{-50}$). |
| 4 | explorer_r2_L00_N07 | Equated exponentially rare completion probability ($\le \exp(-1.12k) \le 10^{-50}$ at $k=100$) with failure probability $\Pr(\mathcal{E}_t)$, erroneously concluding that parallelizing $T = \log^2 k$ threads drives failure to $\exp(-c k \log^2 k)$ when in fact joint failure is $\ge 1 - T \cdot 10^{-50} \approx 1.0$. |
| 5 | explorer_r2_L00_N09 | Inverted single-thread success with failure: with single-thread completion probability $\le \exp(-0.636k)$, thread failure is $1 - o(1) \approx 1.0$, so $T = \lceil\log^2 k\rceil$ parallel renewal threads fail jointly with probability $\ge 1 - T e^{-0.636k} \to 1.0$. |
| 6 | explorer_r2_L00_N10 | Equated exponentially rare path completion with failure probability; running $T = \log^2 k$ threads fails with probability $1 - o(1) \to 1.0$, contradicting claimed super-exponential concentration. |
| 7 | explorer_r2_L00_N15 | Multi-thread scanning claims joint failure $\exp(-c k \log^2 k)$, but true single-thread failure is $\approx 1.0$, rendering multi-thread joint failure $\ge 1 - T e^{-I(C)k} \approx 1.0$. |
| 8 | explorer_r2_L00_N16 | Multi-thread renewal scanning inverts success/failure events; independent threads fail jointly with probability $\ge 1 - T e^{-I(C)k} \to 1.0$. |
| 9 | explorer_r2_L00_N18 | Theorem 4.1 asserts single-thread failure is $\le \exp(-c_1 k)$ and joint failure is $\le \exp(-c_2 k \log^2 k)$, inverting success and failure: single-thread success is $\le 8.96 \times 10^{-51}$ at $k=100$, so joint failure of all $M = \lceil\log^2 k\rceil$ threads is $\ge 1 - M \cdot 10^{-50} \approx 1.000000$. |
| 10 | explorer_r2_L00_N21 | Asserts multi-threaded joint failure is $\le \exp(-\Omega(k \log^2 k))$, but single-thread completion rate is $\lambda = 0.125k$ with success probability $\le 1.94 \times 10^{-49}$ at $k=100$, so all $M$ threads fail with probability $\ge 1 - 10^{-47} \approx 1.0$. |
| 11 | explorer_r2_L00_N22 | Theorem 4.2 claims single-thread conditional failure is $\le e^{-\Omega(k)}$ and joint failure is $\le \exp(-\Omega(k \log^2 k))$, but single-thread success is $\le 1.44 \times 10^{-26}$ at $k=100$ and failure is $\approx 1.0$, resulting in $100\%$ simultaneous thread failure in Poisson simulations (embedding only $26.9\%$ of target elements). |
| 12 | explorer_r2_L00_N25 | Theorem 3.1 claims joint failure of $k$ shifted threads is $\le \exp(-\Omega(k^{5/4}))$, but single-thread arrival rate $\lambda = (C/2)k$ gives success probability $\le 6.89 \times 10^{-53}$ at $k=100$, so all $k$ threads fail with probability $\ge 1 - 6.89 \times 10^{-51} \approx 1.0$. |
| 13 | explorer_r2_L00_N26 | Single-thread 1D strip traversal at height $1/(2k)$ has Poisson arrival rate $\lambda = (C/2)k$, forcing $\mathbb{E}[X_k] = 2/C \approx 8.0 \gg 1.0$. Single thread succeeds with probability $\le \exp(-1.12k) \le 1.94 \times 10^{-49}$ at $k=100$. All $\log^2 k$ threads fail simultaneously with probability $\approx 1.0$, refuting Theorem 5.3's claimed $\exp(-\Omega(k \log^2 k))$ bound. |
| 14 | explorer_r2_L00_N28 | Conflates shift-overlap with physical strip decoupling: adjacent windows with vertical shift $\Delta = 1$ share $k-1$ physical strips ($99.9\%$ sharing at $k=1000$), destroying independent product concentration and invalidating claimed $\exp(-3k \ln k)$ multi-window concentration. |
| 15 | explorer_r2_L00_N29 | Single-strip arrival $\mathbb{E}[X_k] = 1/C = 4.0 > 1.0$ (success $\le 1.23 \times 10^{-29}$ at $k=100$). Proposed multi-row pooling across depth $\Delta = 20$ creates $\Theta(\Delta k)$ vertical rank inversions (mean $294.2$ inversions), destroying pattern isomorphism while multi-threaded failure remains $\approx 1.0$. |
| 16 | explorer_r2_L00_N30 | Theorem 3.2 inverts success and failure: single-step window probability $p \le 0.2404$, thread success is $p^k \le 1.25 \times 10^{-62}$ at $k=100$, and thread failure is $1 - 10^{-62} \approx 1.0$, so all $T$ threads fail almost surely. |
| 17 | strategy_L01_N00 | Single-thread success is $\le 10^{-50}$ so failure is $1 - o(1)$, causing joint failure across $T = \log^2 k$ threads to approach $1.0$; shifted continuous tracks share $>95\%$ host strips, refuting thread independence under FKG correlation. |
| 18 | strategy_L01_N03 | Vertical shifts $\Delta \le \log^2 k$ share $99.8\%$ of host strips, creating strong FKG correlation and causing joint thread failure to approach $1.0$ almost surely. |
| 19 | strategy_L01_N04 | Multi-threaded scanning inverts single-thread failure ($1 - o(1) \approx 1.0$) with success ($\le 10^{-50}$), so joint failure across $T = \log^2 k$ threads approaches $1.0$. |
| 20 | strategy_L01_N06 | Multi-threaded scanning claims joint failure $\exp(-\Omega(k \log^2 k))$, but single-thread success is $\le 10^{-50}$ and failure is $\approx 1.0$, so joint failure of all $\log^2 k$ threads is $1 - o(1) \approx 1.0$. |
| 21 | strategy_L01_N08 | Shifted continuous rank tracks with vertical offset $\Delta \le \log^2 k$ share $95\%$ of host strips, destroying independent product concentration under FKG inequality. |
| 22 | strategy_L01_N09 | Multi-thread diversification fails because structured maps have long shifts and single-thread failure is $\approx 1.0$, rendering joint failure $(1 - p_{\mathrm{succ}})^T \approx 1.0$. |
| 23 | strategy_L01_N10 | Running $T = \lceil\log^2 k\rceil$ parallel threads fails with probability $1 - o(1) \to 1.0$ because single-thread success is exponentially suppressed ($\le 10^{-50}$). |
| 24 | strategy_L01_N12 | Multi-threaded scanning of shifted rank tracks shares $>95\%$ physical strips, causing joint failure across $T = \log^2 k$ threads to approach $1.0$ almost surely. |
| 25 | strategy_L01_N13 | Multi-thread scanning across slabs inverts success and failure probabilities, with joint failure approaching $1.0$ almost surely. |
| 26 | strategy_r2_L02_N02 | Asserts shifted continuous scanning threads achieve joint failure $\le \exp(-\Omega(k \log^2 k))$, but macroscopic corridors of width $w = \Omega(\varepsilon)$ overlap on 100% of target steps for all $\Delta \le 2wk$ (100% thread pairs at $k=1000$), destroying quasi-independence under Harris-FKG inequality. |
| 27 | strategy_r2_L02_N03 | Re-asserts multi-threaded failure bound $\exp(-\Omega(k \log^2 k))$ across $T = \lceil\log^2 k\rceil$ threads despite Theorem 3.3 explicitly proving single threads fail with probability $\ge 1 - 10^{-26} \approx 1.0$ and continuous ribbons share $95\%$ to $99.9\%$ of physical bands. |
| 28 | strategy_r2_L02_N04 | Theorem 6.1 asserts multi-threaded independent failure decay $\exp(-ck \log^2 k)$ at $C=2.5$, but adjacent threads share 99.9% of physical strips ($999/1000$ at $k=1000$), violating independence and inducing strong Harris-FKG positive correlation. |
| 29 | strategy_r2_L03_N00 | Backup Route 1 multi-threaded scanning claim across $T = \lceil\log^2 k\rceil$ threads at $C_{\mathrm{crit}} \ge 2.0$ fails because adjacent continuous ribbons share 99.9% of physical strips $(k-1)/k$, violating independence by Harris-FKG. |
| 30 | strategy_r2_L03_N04 | Backup Route 1 continuous multi-threaded scanning across $T = \lceil\log^2 k\rceil$ threads fails to achieve universality because single-target decay does not overcome the $\ln(k!)$ union bound deficit (+5328.42 nats at $k=1000, C=2.5$) and adjacent continuous ribbons share 99.9% of physical strips. |
| 31 | explorer_r3_L00_N15 | Claiming $T = \ln^2 k$ threads achieve joint failure $\le \exp(-\frac{1}{4} k \ln^2 k)$ inverts single-thread failure ($\approx 1.0$) with rare success ($\le 1.44 \times 10^{-26}$), so true joint failure is $\ge 1 - T \cdot 10^{-26} \approx 1.000$. |
| 32 | N27 (Level 0) | Continuous multi-threaded scanning claims joint failure $\exp(-\Omega(k^{5/4}))$ across $T = \Omega(k^{1/4})$ threads, conflating single-thread success ($\le 2.63 \times 10^{-25}$) with failure and multiplying thread count by thread length in exponent; true joint failure is $1.0 - o(1)$ |
